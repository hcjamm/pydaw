/*This file doesn't do anything, it's only here so that Netbeans will open the entire thing as a project
Each subfolder of the trunk has it's own makefile, and is to be compiled separately, there is no reason to compile
it all together*/

int main(void)
{
	return 0;
}

#For whatever reason, Ubuntu doesn't enable the Universe and Multiverse repositories
#by default on the live DVD image, hence we need to copy a list from an actual install
cp ./sources.list /etc/apt/sources.list

BIN=pydaw4_render
make clean && make gprof && ./$BIN && gprof ./$BIN > profile.txt

if [ -f /usr/bin/gedit ]; then
	gedit profile.txt
elif [ -f /usr/bin/kate ]; then
	kate profile.txt
else
	echo "Now open profile.txt in your favorite editor."
fi

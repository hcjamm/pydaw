if [ -f ./core ]; then
	rm ./core
fi

if [ ! -d dir_debug ]; then
	mkdir dir_debug
else
	rm -rf dir_debug/*
fi

PYDAW_VERSION=$(cat pydaw_version.txt)

make clean && make CFLAGS+=" -DPYDAW_NO_HARDWARE " debug && make PREFIX=$(pwd)/dir_debug install && ./dir_debug/bin/$PYDAW_VERSION --debug --gdb

